# TASK13 - 챗봇 스토리보드 기반 JSON 스키마 고정

## 0. 작업 유형

- 문서 수정

## 1. 작업 목표

- 사용자 스토리보드(자연어 질문 -> LLM 판단 -> DB 분기 -> 최종 응답)를 기준으로
  챗봇 API와 내부 LLM 계약의 JSON 스키마를 고정한다.

## 2. 작업 범위

- `.docs/.task/.KHJ/CHATBOT_API_SPEC.md` 스키마 섹션 개편
- 외부 API 요청/응답 JSON 스키마 명시
- 내부 LLM 1차 계획/2차 답변 생성 계약(JSON) 명시
- 문서 이력(`CHATBOT_PROGRESS`, `WORKLOG`) 동기화

## 3. 작업 제외 범위

- Java 코드 로직 변경
- DB schema/entity 변경
- 인증/필터 정책 변경
- 테스트 코드 변경

## 4. 완료 기준

- [x] 스토리보드 단계(4-1, 5-1~5-6)가 문서 플로우에 반영된다.
- [x] 외부 API 요청/응답 JSON 예시와 필드 규약이 고정된다.
- [x] 내부 LLM 계약(JSON)이 1차 계획/2차 답변 단계로 분리 명시된다.

## 5. 예상 영향 범위

- 대상 레이어: 문서(spec)
- 대상 기능: 챗봇 계약 정의
- 사용자 영향: 없음(개발/협업 기준 정렬)

## 6. 위험도

- 등급: LOW
- 근거: 코드 변경 없는 문서 정리 작업

## 7. 승인 필요 여부

- 필요 여부: 아니오
- 승인 필요 사유: 공개 API 스펙 신규 변경이 아닌 문서 고정/정렬

## 8. 작업 Workflow (필수)

### 8.1 단계 정의

| 단계 | 목표 | 입력 | 출력 | 검증 |
|---|---|---|---|---|
| 1 | 스토리보드를 계약 관점으로 구조화 | 사용자 플로우 설명 | 단계별 계약 초안 | 단계 누락 여부 점검 |
| 2 | API/LLM JSON 스키마 문서 반영 | 기존 `CHATBOT_API_SPEC.md` | 개편된 스키마 문서 | 스키마 일관성 점검 |
| 3 | 문서 이력 동기화 | 변경 내역 | TASK/PROGRESS/WORKLOG 업데이트 | 문서 반영 확인 |

### 8.2 중단 조건

- 범위 외 코드 변경이 요구되는 경우
- 인증/세션 정책 선확정이 필요한 경우

## 9. 검증 계획

- 문서 검토:
  - 외부 API 요청/응답 예시 유효성
  - 내부 LLM 계약 JSON 단계 분리 여부
- 테스트 명령: 없음(문서 작업)

## 10. 결과 기록(작업 후 작성)

- 변경 파일:
  - `.docs/.task/.KHJ/flow/TASK13.md`
  - `.docs/.task/.KHJ/CHATBOT_API_SPEC.md`
  - `.docs/.task/.KHJ/CHATBOT_PROGRESS.md`
  - `.docs/.task/.KHJ/WORKLOG.md`
- 테스트 결과:
  - 문서 작업으로 별도 테스트 미실행
- 미완료 항목(TODO):
  - SQL 실행 보안 정책 상세 문서화
  - 인증/필터 경로 정책 확정 후 반영
- 리스크/주의사항:
  - 현재 스키마는 내부 LLM 계약이며 보안 정책 적용 시 SQL 실행 조건은 변경될 수 있음

<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>TravelMate 관리자 - 게시글 관리</title>
  {{>partials/head-assets}}
  <link href="/css/admin.css" rel="stylesheet">
</head>
<body class="page-body page-body-sans page-admin">
  {{>partials/header}}

  <main class="app-main admin-main">
    <div class="container-xxl">
      <div class="admin-page-shell">
        {{#page}}
        {{>partials/admin-sidebar}}
        {{/page}}

        <section class="admin-content">
          <div class="admin-page-header">
            <div>
              <h1 class="admin-page-title">게시글 관리</h1>
              <p class="admin-page-subtitle">
                커뮤니티의 모든 게시글을 관리하고 모니터링하는 관리자 화면입니다.
              </p>
            </div>
          </div>

          <section class="admin-panel">
            <div class="admin-board-summary">
              <div class="admin-board-summary__left">
                <div class="admin-board-hero__count">
                  <span class="admin-board-hero__count-label">전체 게시글</span>
                  <strong class="admin-board-hero__count-value">{{page.allCount}}</strong>
                </div>
              </div>

              <div class="admin-board-summary__right">
                <a class="admin-board-reset" href="/admin/boards">
                  <span class="material-symbols-outlined">restart_alt</span>
                  필터 초기화
                </a>
              </div>
            </div>

            <div class="admin-board-hero">
              <div class="admin-board-hero__backdrop"></div>

              <div class="admin-board-hero__tab-row">
                <div class="admin-board-hero__tabs admin-tab-shell" aria-label="게시글 카테고리 필터">
                <a href="/admin/boards?category=all{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-all {{#page.allCategoryTab}}is-active-tab{{/page.allCategoryTab}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                   전체
                 </a>

                <a href="/admin/boards?category=tips{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-tips {{#page.isTips}}is-active-tab{{/page.isTips}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                  여행 팁
                </a>

                <a href="/admin/boards?category=plan{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-plan {{#page.isPlan}}is-active-tab{{/page.isPlan}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                  여행 계획
                </a>

                <a href="/admin/boards?category=food{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-food {{#page.isFood}}is-active-tab{{/page.isFood}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                  맛집/카페
                </a>

                <a href="/admin/boards?category=review{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-review {{#page.isReview}}is-active-tab{{/page.isReview}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                  숙소 후기
                </a>

                <a href="/admin/boards?category=qna{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                   class="admin-tab-button cat-qna {{#page.isQna}}is-active-tab{{/page.isQna}}"
                   style="flex:1 1 0; min-width:120px; text-align:center;">
                  질문/답변
                </a>
                </div>
              </div>

              <div class="admin-board-hero__controls">
                <form action="/admin/boards" method="get" class="admin-search-wrap admin-board-search">
                  <span class="material-symbols-outlined">search</span>
                  <input type="hidden" name="category" value="{{page.allCategory}}">
                  <input type="hidden" name="sort" value="{{page.sort}}">
                  <input
                    type="text"
                    name="keyword"
                    value="{{page.keyword}}"
                    placeholder="게시글 검색..."
                    class="admin-board-search__input">
                  <button type="submit" class="admin-search-submit admin-board-search__submit">검색</button>
                </form>

                <div class="admin-board-sort-group">
                  <details class="admin-board-sort-dropdown">
                    <summary class="admin-board-sort-button">
                      {{page.sortFieldLabel}}
                      <span class="material-symbols-outlined">expand_more</span>
                    </summary>
                    <div class="community-sort-menu admin-board-sort-menu">
                      <a class="community-sort-option"
                         href="/admin/boards?sort={{page.viewField}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}">조회순</a>
                      <a class="community-sort-option"
                         href="/admin/boards?sort={{page.likesField}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}">좋아요순</a>
                      <a class="community-sort-option"
                         href="/admin/boards?sort={{page.dateField}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}">날짜순</a>
                    </div>
                  </details>

                  <a href="/admin/boards?sort={{page.toggleDirectionSort}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}"
                     class="admin-filter-button admin-board-sort-toggle">
                    {{page.sortDirectionLabel}}
                  </a>
                </div>
              </div>
            </div>

            <div class="admin-table-wrap">
              <table class="table admin-table">
                <thead>
                  <tr>
                    <th>No</th>
                    <th>제목</th>
                    <th>작성자</th>
                    <th>작성일</th>
                    <th class="text-center">조회수</th>
                    <th class="text-end">관리</th>
                  </tr>
                </thead>
                <tbody>
                  {{#page.boards}}
                  <tr>
                    <td>
                      <span class="admin-category-badge {{categoryClass}}">{{category}}</span>
                    </td>
                    <td>
                      <a href="/boards/{{id}}" class="admin-name admin-title-link">{{title}}</a>
                    </td>
                    <td>{{userName}}</td>
                    <td>{{createdDate}}</td>
                    <td class="text-center">{{viewCount}}</td>
                    <td class="text-end">
                      <div class="admin-action-menu" data-admin-action-menu>
                        {{>partials/admin-board-action-menu}}
                      </div>
                    </td>
                  </tr>
                  {{/page.boards}}

                  {{^page.boards}}
                  <tr>
                    <td colspan="6" class="text-center">게시글이 없습니다.</td>
                  </tr>
                  {{/page.boards}}
                </tbody>
              </table>
            </div>

            <div class="admin-panel__footer">
              <div class="admin-pagination">
                {{#page.prevPage}}
                <a class="admin-page-button"
                   href="/admin/boards?page={{page.prevPage}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}">
                  <span class="material-symbols-outlined">chevron_left</span>
                </a>
                {{/page.prevPage}}

                {{#page.pageItems}}
                  {{#current}}
                  <button class="admin-page-button is-active-page" type="button">
                    {{displayNumber}}
                  </button>
                  {{/current}}

                  {{^current}}
                  <a class="admin-page-button"
                     href="/admin/boards?page={{page}}{{#selectCategory}}&category={{selectCategory}}{{/selectCategory}}{{#sort}}&sort={{sort}}{{/sort}}{{#keyword}}&keyword={{keyword}}{{/keyword}}">
                    {{displayNumber}}
                  </a>
                  {{/current}}
                {{/page.pageItems}}

                {{#page.nextPage}}
                <a class="admin-page-button"
                   href="/admin/boards?page={{page.nextPage}}{{#page.allCategory}}&category={{page.allCategory}}{{/page.allCategory}}{{#page.sort}}&sort={{page.sort}}{{/page.sort}}{{#page.keyword}}&keyword={{page.keyword}}{{/page.keyword}}">
                  <span class="material-symbols-outlined">chevron_right</span>
                </a>
                {{/page.nextPage}}
              </div>
            </div>
          </section>
        </section>
      </div>
    </div>
  </main>

  {{>partials/footer}}
  {{>partials/scripts}}
  {{>partials/admin-page-scripts}}
</body>
</html>
